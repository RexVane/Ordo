{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ordo.fullname" . }}
  labels:
    {{- include "ordo.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ordo.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
{{- end }}
